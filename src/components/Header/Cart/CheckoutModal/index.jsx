import React from "react";
import ModalWrapper from "../../../../utils/ModalWrapper";

function CheckoutModal({ toggleModal }) {
  return <ModalWrapper toggleModal={toggleModal}>checkout modal</ModalWrapper>;
}

export default CheckoutModal;
